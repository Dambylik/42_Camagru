# msmtp — sendmail replacement; routes all mail to Mailpit (dev only)
defaults
    auth           off
    tls            off
    logfile        /var/log/msmtp.log

account        mailpit
    host           mailpit
    port           1025
    from           noreply@camagru.local

account default: mailpit
